name: PR Linters and Test
on:
  push:
    branches:
      - 'equipo_1_jesus'
jobs:
  build:
    runs-on: ubuntu-latest
    strategy: 
      matrix:
        node-version: [12.x]
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: '12.x'
        
      - name: Instalar dependencias
        working-directory: front
        run: npm ci
      
      - name: Ejecutar los test
        working-directory: front
        run: npm run test:ci
    
      - name: Instalar dependencias api
        working-directory: api
        run: npm ci

      - name: Ejecutar los test api
        working-directory: api
        run: npm run test

      - name: Build
        working-directory: front
        run: npm run build
      - name: Archive Production Artifact
        uses: actions/upload-artifact@master
        with:
          name: build
          path: front/build

  deploy:
    name: Deploy
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@master
      - name: Download Artifact
        uses: actions/download-artifact@master
        with:
          name: build
          path: front/build
      - name: Deploy to Firebase
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting
        env:
          FIREBASE_TOKEN: 1//033hUFQlftw7BCgYIARAAGAMSNwF-L9IrXf-_81EVan-laBEF-__ZoGSYh1G5wON1DLhRVbtHsMv0mQ551RfOWmIkqTdTsWbadEs